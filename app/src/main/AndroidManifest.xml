<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="br.com.pk.projetomobilep2">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".HomeMobile">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".basic" />
        <activity android:name=".HomePessoa" />
        <activity android:name=".FormPessoa" />
        <activity android:name=".HomeProduto" />
        <activity android:name=".HomeFunc" />
        <activity android:name=".FormFunc" />
        <activity android:name=".FormProd"></activity>
    </application>

</manifest>